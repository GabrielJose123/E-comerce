import Down from '../assets/Down.svg'

const HandItems = ({ children }) => {
  return (
        <div className="HandManItems">
            Categorias
            <img style={{marginTop:'0.2rem'}} src={Down} alt="" />
        </div>
  );
};

export default HandItems;
